{
    "name": "nextsb",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev",
        "clean": "rm -rf .next build/* docs/*",
        "build": "npm run clean && next build",
        "start": "next start",
        "export": "npm run build && next export -o docs",
        "serve": "http-server -p 3000 ./docs"
    },
    "dependencies": {
        "next": "10.1.3",
        "react": "17.0.2",
        "react-dom": "17.0.2"
    },
    "devDependencies": {
        "@types/node": "^14.14.41",
        "@types/react": "^17.0.3",
        "http-server": "^0.12.3",
        "prettier": "^2.2.1",
        "sass": "^1.32.11",
        "typescript": "^4.2.4"
    }
}
